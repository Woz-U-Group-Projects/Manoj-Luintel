import React, {useState} from 'react';
import {Button, Form, Modal} from "react-bootstrap";

const AddProductModal = ({show}) => {
    const {showModel,setShowModel} =useState(show);


    return (
       <>

       </>
    );
};

export default AddProductModal;
